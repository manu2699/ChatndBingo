{
	"name": "chatndbingo",
	"version": "1.0.0",
	"description": "Chat and play Bingo with your friends",
	"main": "server.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node server.js",
		"heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm isntall --prefix client && npm run build --prefix client"
	},
	"author": "Maneesh K",
	"license": "ISC",
	"dependencies": {
		"express": "^4.17.1",
		"nedb": "^1.8.0",
		"redis": "^3.0.2",
		"socket.io": "^2.3.0"
	}
}
